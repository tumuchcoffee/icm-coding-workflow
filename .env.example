# .env.example — v0.1.3
# Copy this file to .env and customize for your environment.
# The .env file is git-ignored; this template is checked in.

# SQL Server SA password for local development.
# Must meet SQL Server complexity requirements (8+ chars, uppercase, lowercase, digit, or symbol).
MSSQL_SA_PASSWORD=XXXX
