# Enable Docker For Local Development

Make the changes to this app that will allow a developer to run the code in a Docker container when they need to run or confirm their changes. Group the containers in a single docker network while allowing external access from outside the containers.
